# Runner: AWS Init Script

This script bootstraps the runner VM.

## Notes

- `init-mng.sh` and `init-mng-v2.sh` are identical. we preserve them to prevent disrupting any runner that is making use
  of them.
- `init.sh` is retained for historical purposes but is no longer is use and will be removed in the near.
